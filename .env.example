# VPS / production deployment environment variables
PORT=3000
NODE_ENV=production
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: for front-end absolute URLs if needed later
APP_URL=http://localhost:3000
